import {StyleSheet} from 'react-native';
import {DEVICE} from '../../../constants';

export const IMAGEWIDTH = DEVICE.width / 3;

export const styles = StyleSheet.create({
  container: {
    padding: 10,
    flex: 1,
  },
});
